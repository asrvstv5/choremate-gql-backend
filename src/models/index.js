const { Podcast } = require('./podcast');
const { User } = require('./user');
const { Pod } = require('./pod');
const { Chore } = require('./chore');

module.exports = {
    Podcast,
    User,
    Pod,
    Chore
}